<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="haru.park.mapper.BannerMapper">

  <select id="selectActiveOrderBySortOrder" resultType="haru.park.entity.Banner">
    SELECT id, image_url AS imageUrl, link_url AS linkUrl, sort_order AS sortOrder, is_active AS isActive, created_at AS createdAt, updated_at AS updatedAt
    FROM banners
    WHERE is_active = 1
    ORDER BY sort_order, id
  </select>

</mapper>
