<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="haru.park.mapper.UserMapper">

  <resultMap id="UserResultMap" type="haru.park.entity.User">
    <id column="id" property="id" />
    <result column="email" property="email" />
    <result column="name" property="name" />
    <result column="picture" property="picture" />
    <result column="provider" property="provider" />
    <result column="role" property="role" />
    <result column="created_at" property="createdAt" />
  </resultMap>

  <select id="selectByEmail" resultMap="UserResultMap">
    SELECT id, email, name, picture, provider, role, created_at
    FROM users
    WHERE email = #{email}
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO users (email, name, picture, provider, role, created_at)
    VALUES (#{email}, #{name}, #{picture}, #{provider}, #{role}, #{createdAt})
  </insert>

  <update id="update">
    UPDATE users
    SET name = #{name}, picture = #{picture}
    WHERE id = #{id}
  </update>

</mapper>
